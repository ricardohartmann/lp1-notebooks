# Notebooks

Notebooks usados em sala de aula na disciplina Linguagem de Programação I.

## Instalando

```sh
brew install node
pip install pipenv
pipenv install
```

## Executar

```sh
pipenv run jupyter lab
```

ou

```sh
docker-compose up
```
